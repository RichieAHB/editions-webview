{
  "name": "stact",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "parcel-bundler": "^1.12.4",
    "stylis": "^3.5.4"
  },
  "devDependencies": {
    "@types/jest": "^24.0.18",
    "gh-pages": "^2.1.1",
    "jest": "^24.9.0",
    "ts-jest": "^24.1.0",
    "typescript": "^3.6.3"
  },
  "scripts": {
    "start": "parcel index.html",
    "build": "parcel build index.html --public-url /editions-webview/",
    "predeploy": "yarn build",
    "deploy": "gh-pages -d dist"
  }
}
